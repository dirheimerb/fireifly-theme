export interface BreakPoints {
  sm?: string;
  md: string;
  lg?: string;
  xl?: string;
}

const breakpoints: BreakPoints = {
  sm: '30em',
  md: '48em',
  lg: '62em',
  xl: '80em'
};
export default breakpoints;
